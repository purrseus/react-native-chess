export enum SquareColor {
  Light,
  Dark,
}

export enum ChessPieceColor {
  White,
  Black,
}

export enum ChessPieceType {
  King,
  Queen,
  Bishop,
  Knight,
  Rook,
  Pawn,
}

export enum Turn {
  Our,
  Enemy,
}

export enum MoveType {
  Standard,
  EnPassant,
  Promotion,
  Castling,
}
